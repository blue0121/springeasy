spring:
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      timeout: 60s
      password:
      client-type: jedis
      jedis:
        pool:
          enabled: true
          max-active: 50
          max-idle: 8
          min-idle: 1
          max-wait: 60s

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:h2:mem:testdb
    username: root
    password: ""
  sql:
    init:
      mode: always
      schema-locations: classpath:/sql/postgresql/*.sql


